        {
            "name": "submit_imagine_mission",
            "description": "Submit a mission to the Imagine API",
            "parameters": {
                "type": "object",
                "properties": {
                    "prompt": {
                        "type": "string",
                        "description": "prompts transfer to midjourey bot to imagine a picture, e.g. Cat and Dog"
                    }
                },
                "required": [
                    "prompt"
                ]
            }
        }